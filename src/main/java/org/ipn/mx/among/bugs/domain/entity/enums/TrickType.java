package org.ipn.mx.among.bugs.domain.entity.enums;

public enum TrickType {
    REDUCE_TIME,      // Reducir el tiempo a la mitad
    UPSIDE_DOWN,      // Voltear el texto de cabeza (invertir verticalmente)
    REVERSE_TEXT,     // Invertir la cadena de texto (espejo)
    SHUFFLE_WORDS     // Mezclar el orden de las palabras
}
